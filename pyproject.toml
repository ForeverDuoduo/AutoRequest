[project]
name = "autorequest"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.40.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
